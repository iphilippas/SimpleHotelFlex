<%= simple_form_for(@reservation_cost) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :reservation_id %>
    <%= f.input :rate %>
    <%= f.input :day %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
